language: node_js
node_js:
  - "9.11.1"
dist: trusty
cache: npm
before_install:
  - npm i -g npm@6.0.1
  - npm --version
branches:
  only:
    - master
env:
  - TEST_SUITE=test
  - TEST_SUITE=test-security
script:
  - npm run $TEST_SUITE
